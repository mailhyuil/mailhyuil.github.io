# python module & library

```py
import 함수

import 함수 as 별칭

from 모듈 import 함수
```

## **init**.py

> 파이썬은 폴더를 모듈로 읽음
>
> > 폴더 안에 **init**.py 파일이 있어야함
> >
> > > **init**.py 안에 **all** = [함수1, 함수2]를 넣어놓으면 \*로 전부 import 가능

## **main**

> main 스택의 이름

## **name**

> 현재 모듈의 이름
