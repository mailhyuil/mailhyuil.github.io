# kube Helm upgrade

```sh
helm upgrade [release name] [chart name] --set [key]=[value] --set [key]=[value] --set [key]=[value] ...
helm upgrade -f values.yaml -f values2.yaml [release name] [chart name]
```
