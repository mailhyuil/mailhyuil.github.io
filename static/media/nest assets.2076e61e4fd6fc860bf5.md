# nestjs assets

> writeFile로 쓰기 경로 : join(\_\_dirname, 'assets', 'footer.json') // 빌드된 폴더에 생성 됨 (dist)
>
> > fetch로 읽기 fetch('http://localhost:3000/footer.json')
> >
> > > assets에 있는 파일은 /blahblah로 읽을 수 있음
