# npx

> Running executables inside your node_modules/.bin folder
>
> > 디렉토리의 node_modules를 찾아서 .bin 내의 커맨드 실행
> >
> > > node_modules가 없다면 전역 캐시에 설치

```sh
npx <command>
npx <command> --yarn

yarn dlx <command>

pnpm dlx <command>
pnpm exec <command>
pnpx <command>
```
