# chatgpt fine tuning

> 미세조정이라는 의미
>
> > 모델의 파라미터(가중치..)를 미세하게 조정하는 행위
> >
> > > 기존에 학습되어져 있는 모델을 기반으로 아키텍쳐를 새로운 목적(나의 이미지 데이터에 맞게)변형하고 이미 학습된 모델 Weights로 부터 학습을 업데이트하는 방법

## 프롬프트 엔지니어링

> 내부 가중치는 변경하지 않고, 입력(prompt) 조작

## 파인튜닝

## LoRA (Lightweight Optimized ReLU Activation)

> 모델의 일부 가중치만 업데이트

## RAG (Retrieval Augmented Generation) (람다 호출)
