# linux base thread

## kernel-level thread (os thread)

> os에 의해 관리되는 thread
>
> > context switch가 느리다

## user-level thread (user thread)

> user(application)에 의해 관리되는 thread
>
> > context switch가 빠르다
