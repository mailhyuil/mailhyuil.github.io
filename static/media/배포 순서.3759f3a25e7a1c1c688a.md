# 배포

1. 서버에 clone
2. build
3. ecosystem.config.js NOTRO_PORT 정해주기 // notion에 안 물려있는 포트 확인
4. pm2 start ecosystem.config.js
5. nginx available 폴더에 config 파일 생성
6. nginx enabled 폴더에 심볼릭 링크 생성 // sudo ln samil.lepisode.team /etc/nginx/sites-enabled/samil.lepisode.team
7. sudo service nginx restart
