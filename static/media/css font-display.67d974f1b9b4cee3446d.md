# css font-display

```css
font-display: auto | block | swap | fallback | optional

# swap : 기본 폰트를 보여주고 다운로드가 완료되면 교체
# block : 다운로드가 완료될 때까지 폰트를 보여주지 않음
# fallback : 100ms간 폰트를 보여주지 않고 넘으면 기본 폰트로 교체
# auto : 브라우저가 선택
```
