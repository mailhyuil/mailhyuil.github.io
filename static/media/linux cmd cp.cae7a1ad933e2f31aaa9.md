# linux cmd cp

```sh
# file
cp file.txt /usr/local/hello.txt # file.txt가 /usr/local/hello.txt로 복사됨
cp file.txt /usr/local # file.txt가 /usr/local/file.txt로 복사됨

# directory
cp dir /usr/local/dir # dir 폴더가 복사됨
cp dir /usr/local # dir 안의 파일이 local안으로 복사됨
```
