# 표준 스트림

> stdin, stdout, stderr
>
> > 리눅스에서는 모든 것이 파일이다
> >
> > > 프로세스가 시작되면 3개의 파일이 자동으로 열린다
> > >
> > > > 만약 파일을 open한다면 4번째 파일 테이블에 추가된다.

## 파일 디스크립터

```
표준 입력 스트림(standard input, stdin) = 0
표준 출력 스트림(standard output, stdout) = 1
표준 에러 스트림(standard error, stderr) = 2
```
