# next routing

> <Link> 또는 useRouter() 사용 client side routing

## useRouter()

```tsx
import { useRouter } from "next/navigation";

const router = useRouter();
```

## 동적 라우팅

> 폴더 이름에 ()을 붙이면 skip
>
> > 폴더 이름에 []를 붙이면 동적 라우팅
