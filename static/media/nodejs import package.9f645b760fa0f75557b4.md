# nodejs import package

> 패키지를 import 시 global path를 사용하면 node_modules에서 찾는다
>
> > cjs를 사용 시에는 package.json의 main 속성에 있는 파일을 가져옴
> >
> > > mjs를 사용 시에는 package.json의 module 속성에 있는 파일을 가져옴
> > >
> > > > index.d.ts 파일은 package.json의 types 속성에 있는 파일을 가져옴
