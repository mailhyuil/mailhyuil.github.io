# js Date Unix Timestamp

```js
Date.now(); // 현재 시간을 Unix Timestamp로 반환
+new Date(); // 현재 시간을 Unix Timestamp로 반환
new Date().valueOf(); // 현재 시간을 Unix Timestamp로 반환
new Date().getTime(); // 현재 시간을 Unix Timestamp로 반환
```
