# nuxt base cli

nuxt와 nuxi는 같은 명령어

```sh
nuxt build # 빌드
nuxt dev # 개발자 실행
nuxt generate # 모든 페이지를 미리 HTML 파일로 생성하는 SSG(Static Site Generation) 방식 # .output/public 생성
nuxt preview # build 결과물을 실행
nuxt prepare # Nuxt의 타입 시스템(TypeScript)과 자동 가져오기(Auto-imports) 설정을 동기화 # .nuxt 생성
```
