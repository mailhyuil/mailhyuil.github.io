# c++ range for

```cpp
for (int i : {1, 2, 3, 4, 5}) {
  std::cout << i << std::endl;
}

std::vector<int> v = {1, 2, 3, 4, 5};
for (int i : v) {
  std::cout << i << std::endl;
}

// auto
for (auto i : {1, 2, 3, 4, 5}) {
  std::cout << i << std::endl;
}
```
