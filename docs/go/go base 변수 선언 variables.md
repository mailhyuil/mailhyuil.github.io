# go base 변수 선언 variables

```go
const name = "hyuil"
const name string = "hyuil"
const (
    name = "hyuil"
	port = ":8080"
)

var name string= "hyuil" // js의 let과 같음
var name = "sb"
var (
    name = "hyuil"
    age = 10
)
name := "hyuil" // = var name string = "hyuil"

import "fmt"
import (
    "fmt"
    "user"
    t "test"
)
```
