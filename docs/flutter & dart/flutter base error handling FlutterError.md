# flutter error handling FlutterError

## FlutterError.reportError()

- 내부에서 onError 호출, presentError 호출

## FlutterError.presentError()

- 에러를 UI/Console에 출력한다.
- 디버그 모드에서만 동작한다.

## FlutterError.dumpErrorToConsole()

- stack trace를 출력한다.
- UI에는 출력하지 않는다.
