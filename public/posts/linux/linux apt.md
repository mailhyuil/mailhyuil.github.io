# linux apt

> 우분투에서 패키지를 설치

```sh
apt update
apt install <package>
apt remove <package>
apt search <package>

# apt-get
apt-get install <package>
apt-get remove <package>
apt-get search <package>
```
