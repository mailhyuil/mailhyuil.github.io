# linux make

> 소프트웨어를 빌드하는데 사용되는 빌드 도구
>
> > makefile을 통해 빌드 과정을 기술
> >
> > make를 사용하면 Makefile을 읽어서 빌드 과정을 자동으로 수행
> >
> > > 특히 C 및 C++ 프로그램을 컴파일하고 라이브러리를 빌드하는 데 많이 활용

```sh
apt install make -y
```
