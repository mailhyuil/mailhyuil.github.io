# aws cloudfront

> 생성 후 policy를 복사하여 s3 버킷에 붙여넣기
>
> > cloudfront_domain/<s3-resource-location>로 접근 가능

## Viewer

> 클라이언트
>
> > (e.g. browser, mobile ...)

## Origin

> 원본 서버
>
> > (e.g. s3, ec2, elb ...)
