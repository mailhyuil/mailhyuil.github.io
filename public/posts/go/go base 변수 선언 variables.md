# go 변수 선언

```go
const name = "hyuil"
const name string = "hyuil"
const (
    NAME = "hyuil"
	ADDR = ":8080"
)

var name string= "hyuil" // js의 let과 같음
var name = "sb"
name := "hyuil" // = var name string = "hyuil"
```
