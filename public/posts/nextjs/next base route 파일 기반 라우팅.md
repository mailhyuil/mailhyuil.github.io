# next routing 파일 기반 라우팅

> NextJs는 react-route-dom을 사용하지 않고 "app" 폴더 내부의 파일 기반 라우팅을 사용한다.
>
> > 폴더 이름에 ()을 붙이면 skip
> >
> > > 폴더 이름에 []를 붙이면 동적 라우팅

```txt
app/blog/[id].tsx

app/(client)/posts/1
```
