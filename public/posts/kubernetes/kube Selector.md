# kube Selector

## podSelector

> NetworkPolicy에서 사용
>
> > 특정 pod에 대한 정책을 정의할 때 사용

## nodeSelector

> Pod에서 사용
>
> > 특정 노드에 pod를 배치할 때 사용

## namespaceSelector

> NetworkPolicy에서 사용
>
> > 특정 namespace에 대한 정책을 정의할 때 사용

## labelSelector (--selector) (spec.selector in yaml file)

## fieldSelector
