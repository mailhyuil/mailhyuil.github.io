# c++ 참조자 & (reference)

> c++에 새로 도입된 개념
>
> C 언어에서는 어떠한 변수를 가리키고 싶을 땐 반드시 포인터를 사용해야만 했다.
>
> > 참조자란 대상에게 또 다른 이름을 붙여주는 것
> >
> > > 원본과 참조자은 같은 주소값을 가지고 있다.
> > >
> > > > && 를 사용하면 Rvalue 참조자를 만들 수 있다.

```cpp
int num = 1;
int& ref = num;


int&& rref = 1;
```
