# getStarted

## install
```shell
# 전역으로 설치
npm i -g @nestjs/cli
# nest project 생성
nest new project-name
```

## nest cli 기능
> nest 커맨드를 입력하면 기능을 볼 수 있다
```shell
nest generate controller # 컨트롤러를 생성해주는 기능
nest generate service # 서비스를 생성해주는 기능
```