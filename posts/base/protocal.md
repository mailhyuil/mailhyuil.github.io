# 프로토콜

> HTTP, HTTPS, MQTT, AMQP(RabbitMQ), NATS, Websocket, TCP(Kafka), RESP protocol(redis), Postgres protocol는 프로토콜
>
> > gRPC는 프레임워크 (RPC : 원격 프로시저(메소드) 호출) (프로시저는 함수나 메소드와 비슷한 용어)
> >
> > > RPC는 기존에 SOAP 프로토콜을 사용해서 구현하기도 했다
