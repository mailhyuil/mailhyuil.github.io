# 대칭키 & 비대칭키(공개키)

## 대칭키

> 암호화에 사용되는 키와 복호화에 사용되는 키가 동일

## 비대칭키(공개키)

> 두개의 키를 갖음
>
> > A키를 사용해 암호화를 하면 B키로 복호화
> >
> > > B키를 사용해 암호화를 하면 A키로 복호화
> > >
> > > > 클라이언트가 A키를 가지고 있고 서버가 B키를 가지고 있으면
> > > > 클라이언트가 암호화를 하면 서버는 복호화를 할 수 있다
